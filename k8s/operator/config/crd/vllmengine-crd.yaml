# VLLMEngine CRD
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: vllmengines.vllm.io
spec:
  group: vllm.io
  names:
    kind: VLLMEngine
    listKind: VLLMEngineList
    plural: vllmengines
    singular: vllmengine
  scope: Namespaced
  versions:
    - name: v1
      served: true
      storage: true
      subresources:
        status: {}
      additionalPrinterColumns:
        - name: Model
          type: string
          jsonPath: .spec.model
        - name: Replicas
          type: integer
          jsonPath: .spec.replicas
        - name: Ready
          type: integer
          jsonPath: .status.readyReplicas
        - name: URL
          type: string
          jsonPath: .status.serviceURL
        - name: Age
          type: date
          jsonPath: .metadata.creationTimestamp
      schema:
        openAPIV3Schema:
          type: object
          properties:
            apiVersion:
              type: string
            kind:
              type: string
            metadata:
              type: object
            spec:
              type: object
              required:
                - model
              properties:
                model:
                  type: string
                  description: "Model name to serve"
                replicas:
                  type: integer
                  description: "Number of replicas"
                image:
                  type: string
                  description: "Container image"
                gpuResource:
                  type: object
                  properties:
                    count:
                      type: integer
                      description: "Number of GPUs"
                    memory:
                      type: string
                      description: "GPU memory"
                maxModelLen:
                  type: integer
                  description: "Maximum model length"
                tensorParallelSize:
                  type: integer
                  description: "Tensor parallelism degree"
                args:
                  type: array
                  items:
                    type: string
            status:
              type: object
              properties:
                replicas:
                  type: integer
                readyReplicas:
                  type: integer
                availableReplicas:
                  type: integer
                serviceURL:
                  type: string
                conditions:
                  type: array
                  items:
                    type: object
