apiVersion: v1
kind: ConfigMap
metadata:
  name: vllm-config
  namespace: vllm
data:
  production.yaml: |
    server:
      host: 0.0.0.0
      port: 8000
    metrics:
      enabled: true
      port: 9090
