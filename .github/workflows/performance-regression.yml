name: Performance Regression

# Disabled until criterion benchmark sources are restored in the workspace.
on:
  workflow_dispatch:

jobs:
  benchmark:
    name: Performance Regression Check
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Benchmark placeholder
        run: |
          echo "Performance regression checks require benchmark targets."
          echo "Restore benchmark crates before re-enabling PR/push triggers."
          exit 0
