name: Benchmarks

# Benchmark sources are not yet wired in the workspace; run manually when restored.
on:
  workflow_dispatch:

jobs:
  benchmark:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Run benchmarks
        run: |
          echo "No benchmark targets are configured in this workspace."
          echo "Re-enable this workflow after restoring crates/benches or core/benches sources."
          exit 0
