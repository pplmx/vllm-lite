---
phase: "17.2"
status: "passed"
date: "2026-05-13"
---

# Phase 17.2 Verification: Self-Speculation Forward Pass

## Success Criteria Verification

| # | Criterion | Status | Evidence |
|---|-----------|--------|----------|
| 1 | SelfSpeculativeModel.generate_draft() runs a real forward pass through 1/8 layers with weight sharing | ✓ | `generate_draft()` calls `model.forward_to_layer(upto_layer=draft_layer_count)` — real forward with layer truncation (self_spec.rs:170-196) |
| 2 | Draft generation uses greedy argmax sampling | ✓ | `forward_to_layer()` implementations use argmax on last logits position (qwen3/model.rs:742-769, qwen3_5/model.rs:313-322, qwen3_5/hybrid.rs:1138-1147) |
| 3 | Draft and target maintain separate KV cache block IDs | ✓ | Draft tracks own `draft_kv_block_ids: HashMap<SeqId, Vec<usize>>` — initialized from target batch on first use, maintained independently across draft steps (self_spec.rs:155-158, line 197 removed) |
| 4 | Weight sharing uses zero-copy references | ✓ | `SelfSpeculativeModel` wraps same model instance; `forward_to_layer(upto_layer=N)` limits layer depth — no additional memory allocated (self_spec.rs:14-19) |

## Sub-Plan Completion

| Plan | Description | Status |
|------|-------------|--------|
| 17.2-A | Add `forward_to_layer()` to ModelBackend trait | ✓ Already existed on trait with default fallback (traits/model.rs:105-127) |
| 17.2-B | Implement for concrete models | ✓ Qwen3 (existing), Qwen3.5 model, Qwen3.5 hybrid added; 8 stub models use default fallback |
| 17.2-C | Draft KV cache isolation | ✓ Fixed: removed `*draft_block_ids = batch.kv_block_ids.clone()` — draft maintains own block IDs |
| 17.2-D | Real generate_draft() implementation | ✓ Already implemented — uses `model.forward_to_layer()` with per-sequence per-position loop |
| 17.2-E | Weight sharing (zero-copy) | ✓ Implicit via single model instance with layer truncation |
| 17.2-F | Tests | ✓ 10 existing tests pass; forward_to_layer fallback tested via TrackingModel |

## Build Verification

- `cargo build --workspace`: ✓ passes
- `cargo test -p vllm-core --lib -- speculative`: ✓ 45 passed, 0 failed
- `cargo test -p vllm-model --lib`: ✓ 363 passed, 0 failed
- `cargo clippy --workspace -- -D warnings`: ✓ clean

## Files Changed

| File | Change |
|------|--------|
| `crates/core/src/speculative/self_spec.rs` | Fixed draft KV isolation: removed block ID copy from target |
| `crates/model/src/qwen3_5/model.rs` | Added `forward_to_layer()` with layer truncation |
| `crates/model/src/qwen3_5/hybrid.rs` | Added `forward_to_layer()` with layer truncation |

## Verdict

**PASSED** — All 4 success criteria met. Real layer-truncated forward pass works. Draft KV cache independently tracked. No weight duplication.
