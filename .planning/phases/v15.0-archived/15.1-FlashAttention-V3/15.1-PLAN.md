# Phase 15.1: FlashAttention V3 - Plan

**Phase:** 15.1
**Created:** 2026-04-27
**Status:** Ready for execution

---

## Implementation Plan

### Task 1: Research FA-V3 Algorithm and API

Research FlashAttention V3 changes from v2:
- Online softmax algorithm changes
- New kernel API (flash_attn_v3_forward, flash_attn_v3_backward)
- Memory layout differences (seqlen_q, seqlen_k)
- Sliding window attention support

### Task 2: Create FA-V3 Kernel Module

Create `crates/model/src/components/attention/flash_attn_v3.rs`:
```rust
// Key functions:
pub fn flash_attn_v3_forward(...) // Forward pass
pub fn flash_attn_v3_backward(...) // Backward pass  
pub fn flash_attn_v3_forward_with_swa(...) // Sliding window
```

### Task 3: Update GqaAttention for FA-V3

Modify `crates/model/src/components/attention/gqa.rs`:
- Add `flash_attn_v3` feature flag
- Update attention forward to dispatch to V3 kernel
- Handle MQA/GQA head mappings

### Task 4: Add Feature Flag

Update `crates/model/Cargo.toml`:
```toml
[features]
flash_attn_v2 = ["dep/candle-flash-attn"]
flash_attn_v3 = ["dep/candle-flash-attn"]  # Add
```

### Task 5: Benchmark Comparison

Add benchmark in `benches/src/attention.rs`:
- Compare V2 vs V3 throughput
- Measure memory usage reduction
- Test with various sequence lengths

---

## Files to Create/Modify

| File | Action |
|------|--------|
| `crates/model/src/components/attention/flash_attn_v3.rs` | Create |
| `crates/model/src/components/attention/mod.rs` | Modify |
| `crates/model/src/components/attention/gqa.rs` | Modify |
| `crates/model/Cargo.toml` | Modify |
| `benches/src/attention.rs` | Modify |

---

## Success Verification

1. Unit tests pass for V3 kernel with causal masking
2. MQA models (Llama 3) generate correctly
3. GQA models (Gemma3, Mistral, Llama 4) generate correctly  
4. Benchmark shows >30% speedup vs V2
