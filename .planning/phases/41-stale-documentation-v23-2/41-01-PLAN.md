---
phase: 41
plan: 01
title: Rewrite CLAUDE.md and fix README scheduling example
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
  - README.md
autonomous: true
---

# Phase 41 Plan 01: Rewrite CLAUDE.md and Fix README Scheduling Example

**Goal:** Rewrite `CLAUDE.md` to reflect v23.0 reality (DOC-02) and fix the
broken scheduling policy example in `README.md:459-473` (DOC-03). These are
the two highest-visibility doc fixes — `CLAUDE.md` is read by every Claude
Code session, and the README example fails to compile.

<tasks>

<task type="auto">
<name>Rewrite CLAUDE.md + fix README scheduling example imports</name>
<read_first>
- CLAUDE.md (current stale content)
- README.md (lines 459-473 — broken scheduling example)
- crates/core/src/scheduler/policy/mod.rs (canonical policy re-exports)
- crates/core/src/engine.rs:45 (Engine struct shape — non-generic, Box<dyn>)
- Cargo.toml (workspace members — verify 6 crates)
- rust-toolchain.toml (or CI config — verify Rust 1.85)
</read_first>
<acceptance_criteria>
1. `CLAUDE.md` no longer says "4 crates" — updated to "6 crates" with all
   six members listed (`vllm-traits`, `vllm-core`, `vllm-model`, `vllm-server`,
   `vllm-dist`, `vllm-testing`)
2. `CLAUDE.md` no longer references Rust 1.75 — updated to current toolchain
   (1.85)
3. `CLAUDE.md` Engine example updated from generic `Engine<M: ModelBackend>`
   to non-generic shape: `Engine { target_model: Box<dyn ModelBackend>, ... }`
4. `CLAUDE.md` no longer references `crates/model/src/qwen3/attention.rs` —
   replaced with valid path (`crates/model/src/components/attention/gqa.rs`
   or current equivalent)
5. `README.md:459-473` scheduling example imports updated from
   `vllm_core::scheduler::{SchedulerEngine, FcfsPolicy, SjfPolicy, PriorityPolicy}`
   to `vllm_core::scheduler::{SchedulerEngine};` plus
   `vllm_core::scheduler::policy::{FcfsPolicy, SjfPolicy, PriorityPolicy}`
6. README example compiles as a snippet (verify by extracting the code block
   and attempting `cargo check` against it; alternatively verify all imports
   resolve)
7. The CLAUDE.md commit message example still references `<type>(<scope>): <subject>`
   (unchanged convention)
8. `cargo doc --workspace --no-deps` still produces 0 broken-link warnings
   (doc-comment cross-references shouldn't break)
</acceptance_criteria>
<action>
1. Read `Cargo.toml` to confirm 6 workspace members (or check the actual list)
2. Read `crates/core/src/engine.rs:45` for the current Engine struct definition
3. Read `crates/core/src/scheduler/policy/mod.rs` for the actual policy imports
4. Rewrite `CLAUDE.md`:
   - Update crate count to 6; list all 6 crates with their roles
   - Update Rust version to 1.85 (or whatever is current per `rust-toolchain.toml`
     or CI workflow)
   - Update Engine example to non-generic `Box<dyn ModelBackend>` shape
   - Remove or correct the `qwen3/attention.rs` reference
   - Preserve the common commands section, dev workflow section, and key
     design decisions section
5. Update `README.md:459-473` (the scheduling policy example):
   - Change import from `vllm_core::scheduler::{SchedulerEngine, FcfsPolicy, SjfPolicy, PriorityPolicy}`
     to `vllm_core::scheduler::SchedulerEngine` + `vllm_core::scheduler::policy::{FcfsPolicy, SjfPolicy, PriorityPolicy}`
   - Verify the rest of the example matches actual `SchedulerEngine::new(...)`
     signature (check `crates/core/src/scheduler/engine.rs`)
   - Verify `engine.set_policy(...)` accepts `Box<dyn SchedulingPolicy>` (it does,
     per `scheduler/engine.rs:94`)
6. Run `cargo doc --workspace --no-deps` to verify no broken-link warnings
7. (Optional but recommended) Extract the README example into a temporary file
   and attempt `cargo check --example` to verify it compiles — but since
   vllm-core's crate name may differ, this may require manual verification
8. Read the rewritten CLAUDE.md to confirm it's coherent
9. Read the updated README.md section to confirm the example is correct
</action>
<verify>
- `CLAUDE.md` mentions "6 crates" (grep: `grep -c "crates" CLAUDE.md` shows ≥6 mentions)
- `CLAUDE.md` no longer mentions Rust 1.75 (grep: `grep "1\.75" CLAUDE.md` returns nothing)
- `CLAUDE.md` no longer references `qwen3/attention.rs` (grep: `grep "qwen3/attention" CLAUDE.md` returns nothing)
- `README.md:459-473` imports match actual scheduler policy module
  (grep: `grep "scheduler::policy" README.md` shows the new path)
- `cargo doc --workspace --no-deps` produces 0 broken-link warnings
</verify>
</task>

</tasks>

<verification>
## must_haves

### truths
- [ ] CLAUDE.md crate count = 6
- [ ] CLAUDE.md Rust version = 1.85 (current)
- [ ] CLAUDE.md Engine signature uses Box<dyn ModelBackend>
- [ ] CLAUDE.md has no broken module references
- [ ] README scheduling example imports resolve

### artifacts
- [ ] CLAUDE.md rewritten
- [ ] README.md scheduling example fixed

### key_links
- [ ] `cargo doc --workspace --no-deps` produces 0 warnings
</verification>

<requirements>
- DOC-02
- DOC-03
</requirements>
