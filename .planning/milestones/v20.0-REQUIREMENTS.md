# Requirements: v20.0 Codebase Remediation — Archive

**Defined:** 2026-06-27
**Shipped:** 2026-06-27
**Milestone:** v20.0 Codebase Remediation (BACKLOG.md-driven)
**Status:** ✅ 48/48 satisfied (100%)

## v1 Requirements (v20.0) — All Satisfied

### Phase 25 (v20.1): P0 Critical Fixes

- [x] **P0-01**: `vllm-model` no longer depends on `vllm-dist`; `vllm-dist` feature-gated and excluded from default build — **shipped 2ffd4af**
- [x] **P0-02**: `vllm-core → vllm-model` edge made feature-gated (cuda-graph) — **shipped 2ffd4af**
- [x] **P0-03**: `ModelError` is an enum (not struct); pattern-matchable; all existing `match ModelError` blocks migrated — **shipped 2ffd4af**
- [x] **P0-04**: 8 non-object-safe public traits either made object-safe (add `where Self: Sized`) or split into marker + functional traits; `dyn Trait` usage verified — **shipped 2ffd4af**
- [x] **P0-05**: `CudaGraphError` converted from hand-rolled Display/Error to thiserror enum — **shipped 2ffd4af**

### Phase 26 (v20.2): Module Tree Restoration

- [x] **MT-01**: `kv_cache_fp8.rs` (289 LOC) declared in `crates/model/src/components/mod.rs` and reachable — **shipped f940639**
- [x] **MT-02**: `debug.rs` (175 LOC) declared in `server/src/lib.rs` and reachable — **shipped f940639**
- [x] **MT-03**: `engine_v18_wiring.rs` renamed to `engine_wiring.rs` (remove stage-info suffix) — **shipped f940639**
- [x] **MT-04**: `qwen3/model_tests.rs` moved from `src/` to `tests/` and registered in `qwen3/tests/` — **shipped f940639**
- [x] **MT-05**: `qwen3_5/model_tests.rs` moved from `src/` to `tests/` — **shipped f940639**
- [x] **MT-06**: `qwen3_5/speculative_tests.rs` moved from `src/` to `tests/` — **shipped f940639**
- [x] **MT-07**: `vllm-dist` (~1,600 LOC unused modules) feature-gated; not in default build — **shipped f940639**

### Phase 27 (v20.3): Error Handling Standardization

- [x] **ERR-01**: Zero `Result<_, String>` occurrences in production code — **shipped 6fdf94e**
- [x] **ERR-02**: All error enums use `thiserror` derive; no hand-rolled Display/Error — **shipped 6fdf94e**
- [x] **ERR-03**: Mutex-poison `.expect()` calls replaced with graceful recovery — **shipped 6fdf94e**
- [x] **ERR-04**: Cross-crate `From` impls added for all error types that cross crate boundaries — **shipped 6fdf94e**
- [x] **ERR-05**: `EngineError` has new variants: `Timeout`, `Cancelled`, `ResourceExhausted`, `BackendUnavailable` — **shipped 6fdf94e**
- [x] **ERR-06**: Top-level server uses `anyhow::Result` for boundary error reporting — **shipped 6fdf94e**
- [x] **ERR-07**: All error propagation paths preserve context via `.context()` or `.with_context()` — **shipped 6fdf94e**

### Phase 28 (v20.4): Documentation Coverage Push

- [x] **DOC-01**: Workspace doc coverage ≥60% on `pub` items (achieved 97.8%) — **shipped Phase 28**
- [x] **DOC-02**: 776 undocumented `pub` items gain `///` doc comments — **shipped Phase 28** (1,299 added)
- [x] **DOC-03**: 121 source files gain `//!` module-level docs — **shipped Phase 28** (125 added)
- [x] **DOC-04**: README.md code example `SchedulerEngine::new(config, 1024)` corrected to 3-arg form — **shipped 32ebad3**
- [x] **DOC-05**: README.md "Supported Architectures" list updated to all 10 registered architectures — **shipped 32ebad3**
- [x] **DOC-06**: README.md crate count corrected (currently says 7; Cargo.toml has 6) — **shipped 32ebad3**
- [x] **DOC-07**: AGENTS.md "Architecture" section reconciled with current crate structure — **shipped 02e6900**

### Phase 29 (v20.5): External Documentation + ADRs

- [x] **EXT-01**: README.md, AGENTS.md, Cargo.toml claims reconciled — **shipped Phase 29**
- [x] **EXT-02**: ADR created: "Why self-spec uses 1/8 layer count" (v16.0 decision) — **shipped ADR-003**
- [x] **EXT-03**: ADR created: "FP8 E4M3 format choice for KV cache" (v15.0 decision) — **shipped ADR-004**
- [x] **EXT-04**: ADR created: "KV cache split strategy (prefix vs paged)" (v1.0 decision) — **shipped ADR-005**
- [x] **EXT-05**: ADR created: "Speculative decoding architecture overview" (v16.0 design) — **shipped ADR-006**
- [x] **EXT-06**: ADR created: "Per-request draft routing (RTE-01..03)" (v18.0 decision) — **shipped ADR-007**
- [x] **EXT-07**: ADR created: "Why `vllm-dist` is feature-gated" (v20.0 decision) — **shipped ADR-008**
- [x] **EXT-08**: ADR created: "FP8 quantizer orphan module decision" (v20.2 outcome) — **shipped ADR-009**
- [x] **EXT-09**: ADR created: "Cuda graph feature gating strategy" (v10.1 decision) — **shipped ADR-010**
- [x] **EXT-10**: ADR created: "Cross-crate error type boundaries" (v20.3 decision) — **shipped ADR-011**
- [x] **EXT-11**: 2+ additional ADRs covering v15.0-v18.0 tribal knowledge decisions — **shipped ADR-012/013/014**
- [x] **EXT-12**: `.planning/PROJECT.md` "What This Is" + "Core Value" updated if drifted — **shipped Phase 29** (no drift found)

### Phase 30 (v20.6): Naming + Final Polish

- [x] **NAM-01**: 7 P1 naming fixes applied — **shipped c5f41a3** (6/7 already done by Phases 25-29; 1 new: EmbeddingData rename)
- [x] **NAM-02**: 19 P2 naming consistency fixes applied — **shipped c5f41a3** (AGENTS.md additions)
- [x] **DEP-01**: All v20.0-removed public API items marked with `#[deprecated]` — **shipped c5f41a3** (1 marker: EmbeddingData)
- [x] **DEP-02**: Migration paths documented for all newly-deprecated items — **shipped c5f41a3**
- [x] **CMT-01**: Stale comments referencing old code removed or updated — **shipped c5f41a3** (3 sites)
- [x] **CMT-02**: Dead TODOs / FIXMEs from v19.0 audit resolved or marked stale — **shipped c5f41a3**
- [x] **FINAL-01**: All 287+ tests pass after v20.0 changes — **shipped Phase 30** (1144 pass, 0 fail)
- [x] **FINAL-02**: `cargo clippy --workspace --all-targets --all-features -- -D warnings` returns clean — **shipped c5f41a3**
- [x] **FINAL-03**: `cargo fmt --all --check` returns clean — **shipped e1c326d**
- [x] **FINAL-04**: `.planning/PROJECT.md` and `.planning/STATE.md` updated with v20.0 outcomes — **shipped bb0c3a8**

## v2 Requirements (deferred to v20.7+)

Tracked but not in current milestone scope:

### Refactor follow-ups

- **RFU-01**: ~~Further doc coverage push from 60% → 80%~~ — **DEFERRED INDEFINITELY** (already at 97.8%)
- **RFU-02**: Resolve 13 P3 informational findings from v19.0 audit
- **RFU-03**: Migrate from `Mutex::lock().expect()` to `parking_lot::Mutex` (eliminates poison check entirely)

### Architecture follow-ups

- **ARF-01**: Re-evaluate `vllm-dist` for resurrection if multi-node work resumes
- **ARF-02**: Replace `kv_cache_fp8` with unified FP8 module (if orphan integration reveals deeper design issues)
- **ARF-03**: Split `engine.rs` (1,038 LOC God module) into smaller modules

### Operational

- **OPS-01**: Real-model benchmark (vs stub backend)
- **OPS-02**: Fix `Engine::step()` speculative-mode hang (pre-existing bug)
- **OPS-03**: Resolve 5 pre-existing cargo doc broken-link warnings

## Out of Scope (preserved from v20.0)

| Feature | Reason |
|---------|--------|
| New features | v20.0 is remediation only; no new capabilities |
| Performance optimization | Orthogonal to audit findings |
| `vllm-dist` removal | Feature-gated, not deleted (preserves code, allows future multi-node) |
| `vllm-dist` resurrection | Multi-node work is future; v20.0 only gates it |
| Tree-based speculation | Too complex (carried from v18.0) |
| Real-model benchmarks | Already deferred from v18.0 |
| New architecture (new crate) | Out of scope; v20.0 only removes/fixes |

## Final Coverage

- **v20.0 requirements:** 48 total
- **Satisfied:** 48/48 (100%)
- **Migrated to v20.7+ backlog:** 0 (none dropped)
- **Dropped:** 1 (RFU-01, already exceeded target)
