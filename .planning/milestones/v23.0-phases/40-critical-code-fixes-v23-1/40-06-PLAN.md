---
phase: 40
plan: 06
title: FINAL-01 verification — 1179+ tests green
wave: 1
depends_on:
  - 40-01
  - 40-02
  - 40-03
  - 40-04
  - 40-05
files_modified: []
autonomous: true
---

# Phase 40 Plan 06: FINAL-01 Verification — 1179+ Tests Green

**Goal:** Confirm the v22.0 invariant `cargo test --workspace --all-features`
reports ≥ 1179 passing tests post-Phase-40. No regression from any of the
5 CODE-* fixes.

<tasks>

<task type="auto">
<name>Run FINAL-01 verification gate</name>
<read_first>
- AGENTS.md ("Verification (Required Before Commit)" section)
- .planning/STATE.md (current test count baseline: 1179)
- justfile (`just nextest` vs `just nextest-all` — skipped vs included)
</read_first>
<acceptance_criteria>
1. `cargo test --workspace --all-features` reports ≥ 1179 tests passing
   (v22.0 baseline; Phase 40 is remediation, no expected growth)
2. `cargo clippy --workspace --all-targets --all-features -- -D warnings` exits 0
3. `cargo fmt --all --check` exits 0
4. `cargo doc --workspace --no-deps` produces zero broken-link warnings
5. The specific test counts per crate are recorded in the SUMMARY.md so the
   delta is auditable (baseline 1179; post-Phase-40 ≥ 1179)
6. If any test count drops below 1179, identify the cause and either:
   - Fix the regression (preferred), or
   - Document the gap explicitly with rationale
</acceptance_criteria>
<action>
1. Read AGENTS.md "Quick Commands" section to confirm the correct test runner
   invocation
2. From the repo root, run `cargo test --workspace --all-features 2>&1 | tee /tmp/phase-40-final01.log`
3. Count tests: extract the trailing summary line (e.g., `test result: ok. 1179 passed; 0 failed; ...`)
4. If `cargo nextest` is available, also run `just nextest` for the canonical
   gated count (skips `#[ignore]` slow tests)
5. Run `cargo clippy --workspace --all-targets --all-features -- -D warnings 2>&1 | tee /tmp/phase-40-clippy.log`
6. Run `cargo fmt --all --check 2>&1 | tee /tmp/phase-40-fmt.log`
7. Run `cargo doc --workspace --no-deps 2>&1 | tee /tmp/phase-40-doc.log`
8. Record the results in the SUMMARY.md:
   - Test count (was 1179, now ≥ 1179)
   - Clippy: clean (or count warnings)
   - Fmt: clean (or count diffs)
   - Doc: 0 broken links (or count warnings)
9. If any gate fails:
   - For test regressions: investigate, fix, re-run
   - For clippy/fmt/doc: fix the introduced issues (likely a missing
     `#[allow(...)]` or a missed format change)
10. If all gates pass, write a one-line confirmation in the SUMMARY.md
    "FINAL-01: PASS (N tests)" where N is the observed count
</action>
<verify>
- Test count from `cargo test --workspace --all-features` ≥ 1179
- `cargo clippy --workspace --all-targets --all-features -- -D warnings` exits 0
- `cargo fmt --all --check` exits 0
- `cargo doc --workspace --no-deps` produces 0 broken-link warnings
- All gates recorded in SUMMARY.md
</verify>
</task>

</tasks>

<verification>
## must_haves

### truths
- [ ] Test count ≥ 1179 (no regression)
- [ ] Clippy clean across workspace + all targets + all features
- [ ] Fmt clean
- [ ] Doc has zero broken-link warnings

### artifacts
- [ ] SUMMARY.md updated with FINAL-01 verification result

### key_links
- [ ] `cargo test --workspace --all-features` passes with ≥ 1179 tests
- [ ] `cargo clippy --workspace --all-targets --all-features -- -D warnings` passes
- [ ] `cargo fmt --all --check` passes
- [ ] `cargo doc --workspace --no-deps` produces 0 broken-link warnings
</verification>

<requirements>
- FINAL-01
</requirements>
