---
phase: 41
plan: 02
title: CHANGELOG backfill + MIGRATING v22 entry + architecture.md
wave: 1
depends_on: []
files_modified:
  - CHANGELOG.md
  - MIGRATING.md
  - docs/architecture.md
autonomous: true
---

# Phase 41 Plan 02: CHANGELOG Backfill + MIGRATING v22 Entry + Architecture Doc

**Goal:** Fill the doc gaps that require synthesis from archived milestone files:
- DOC-04: CHANGELOG.md backfill v19.0/v20.0/v21.0/v22.0 entries
- DOC-05: MIGRATING.md v22.0 entry (security middleware, parking_lot, LazyLock)
- DOC-06: docs/architecture.md — new unified v23.0 architecture overview

<tasks>

<task type="auto">
<name>Backfill CHANGELOG + add MIGRATING v22 entry + create architecture.md</name>
<read_first>
- CHANGELOG.md (current content — find existing v18 entry as style reference)
- MIGRATING.md (find existing entry as style reference)
- .planning/milestones/v22.0-MILESTONE-AUDIT.md (v22.0 outcome reference)
- .planning/milestones/v22.0-phases/36-39 SUMMARY.md files (per-phase detail)
- .planning/milestones/v21.0-ROADMAP.md, v20.0-ROADMAP.md, v19.0-ROADMAP.md
- docs/adr/ (cross-reference for architecture doc)
- crates/core/src/ (verify current module structure for architecture doc)
</read_first>
<acceptance_criteria>
1. CHANGELOG.md contains entries for v19.0, v20.0, v21.0, v22.0
2. Each new entry includes: date, phase count + names, key accomplishments,
   test count at end, tech debt roll-forward
3. MIGRATING.md contains a v22.0 entry covering:
   - Security middleware wiring (JWT verify, RBAC, RequestBodyLimitLayer)
   - parking_lot::Mutex migration (24 sites)
   - LazyLock upgrade (Rust 1.80+ std::sync::LazyLock)
4. MIGRATING v22.0 entry includes example diffs (before/after snippets) for
   at least 2 of the 3 changes
5. docs/architecture.md exists with sections covering:
   - Engine orchestration (split into engine/, engine/spec_dispatch/)
   - Scheduler split (queue/preemption/eviction/batch)
   - Paged tensor split (logical KV cache in core/, physical in model/paged_tensor/)
   - Architecture registry pattern (Architecture trait, registry)
   - Multi-model spec flow (speculative decoding wiring)
6. architecture.md cross-references at least 2 relevant ADRs from `docs/adr/`
7. Existing CHANGELOG and MIGRATING content (pre-v19 entries) is preserved
8. `cargo doc --workspace --no-deps` still produces 0 broken-link warnings
   (verify the new architecture.md doesn't break intra-doc links)
9. Markdown lint sanity: no broken intra-doc anchor links (if any)
</acceptance_criteria>
<action>
1. Read `CHANGELOG.md` to find existing entries (v18.0 likely) as style reference
2. Read each of `.planning/milestones/v{19,20,21,22}.0-ROADMAP.md` and the
   corresponding audit files to extract key accomplishments + test counts
3. Add 4 entries to CHANGELOG.md (between existing v18 entry and any newer
   content). Use a consistent format:
   ```
   ## [vXX.0] - YYYY-MM-DD
   ### Milestone: <Name>
   **Phases:** N (Phase X-Y)
   **Test count:** N
   ### Highlights
   - <3-5 bullets per milestone>
   ### Tech Debt Rolled Forward
   - <carry-over items>
   ```
4. Read `MIGRATING.md` to find existing entries as style reference
5. Add a `## v22.0 — Production Hardening` section to MIGRATING.md with:
   - 3 subsections (Security, parking_lot, LazyLock)
   - Before/after diff snippets (simplified; no need to compile)
   - Link to relevant PRs or commit hashes if known
6. Create `docs/architecture.md` with sections:
   - `# vLLM-lite Architecture (v23.0)`
   - `## Overview` — 1 paragraph
   - `## Engine Orchestration` — covers `crates/core/src/engine.rs` and `engine/spec_dispatch/`
   - `## Scheduler Split` — covers the 5 sub-modules in `crates/core/src/scheduler/`
   - `## KV Cache Layering` — logical (in core/) vs physical (in model/paged_tensor/)
   - `## Architecture Registry Pattern` — `crates/model/src/arch/`
   - `## Multi-Model Spec Flow` — speculative decoding
   - `## ADR References` — link to relevant docs in `docs/adr/`
7. Cross-reference at least 2 ADRs (e.g., ADR-001 Engine split, ADR-008
   multi-node feature-gating, ADR-009 orphan modules) — verify which exist
8. Run `cargo doc --workspace --no-deps` to verify no broken-link warnings
9. Verify file sizes are reasonable (architecture.md: 200-400 lines typical)
</action>
<verify>
- `CHANGELOG.md` mentions v19.0, v20.0, v21.0, v22.0 (grep each)
- `MIGRATING.md` mentions v22.0 entry with the 3 required subsections
- `docs/architecture.md` exists and is > 100 lines
- `cargo doc --workspace --no-deps` exits 0 (0 broken-link warnings)
</verify>
</task>

</tasks>

<verification>
## must_haves

### truths
- [ ] CHANGELOG.md has 4 new entries (v19.0, v20.0, v21.0, v22.0)
- [ ] MIGRATING.md has v22.0 entry with 3 subsections + diff snippets
- [ ] docs/architecture.md exists with 5+ sections

### artifacts
- [ ] CHANGELOG.md modified
- [ ] MIGRATING.md modified
- [ ] docs/architecture.md created

### key_links
- [ ] `cargo doc --workspace --no-deps` produces 0 warnings
</verification>

<requirements>
- DOC-04
- DOC-05
- DOC-06
</requirements>
