---
phase: 42
plan: 01
title: Bulk placeholder doc removal + fix wrong comments + deprecation shim
wave: 1
depends_on: []
files_modified:
  - crates/core/src/lib.rs
  - crates/model/src/lib.rs
  - crates/server/src/lib.rs
  - crates/server/src/health.rs
  - (and ~30+ files with placeholder patterns)
autonomous: true
---

# Phase 42 Plan 01: Placeholder Doc Cleanup + Wrong Comment Fixes

**Goal:** Remove placeholder rustdoc across the codebase (CMT-01, CMT-02, CMT-04),
fix the 4 actively wrong comments (CMT-05), and resolve the `qwen3_config`
deprecation shim (CMT-06). CMT-03 (builder copy-paste docs) is complete-by-
discovery: zero occurrences of the audited pattern remain.

<tasks>

<task type="auto">
<name>Bulk placeholder removal + comment fixes + deprecation cleanup</name>
<read_first>
- .planning/audit/docs/ (audit findings — for reference of what to remove)
- crates/core/src/lib.rs (16-41: module placeholders)
- crates/model/src/lib.rs (3-42: module placeholders; 44-52: qwen3_config shim)
- crates/server/src/lib.rs (3-4: triple-header)
- crates/server/src/health.rs (3-4: triple-header)
- crates/traits/src/types.rs (verify no line 264/273 corruption; already
  shortened by Phase 40 — confirm)
</read_first>
<acceptance_criteria>
1. CMT-01: All `//! X: X.` module-level placeholders deleted across
   `crates/core/src/`, `crates/model/src/components/`, `model/paged_tensor/`,
   `model/kernels/`, and other locations. Verifiable via
   `grep -rEn "^//!\s+[a-z_]+:\s+[a-z_][a-z_ ]+\.?\s*$" crates/` returning 0
2. CMT-02: All `/// X: X.`-style function placeholders deleted (where the doc
   adds no information beyond restating the name). Verifiable via
   `grep -rEn "^///\s+[a-z_]+:\s+[a-z_]+\.?\s*$" crates/` returning 0
3. CMT-03: Confirmed zero occurrences of
   `/// builder: construct via builder for documented field ergonomics.`
4. CMT-04: Phase/audit IDs (`v18.0`, `Plan 17.x`, `SEC-06`, `PERF-01`, `ARF-07`)
   stripped from user-visible rustdoc. Conservative approach: only strip from
   lines that ONLY contain the ID + surrounding noise (e.g., "fixed in v18.0");
   leave lines that reference the audit finding substantively.
5. CMT-05: 4 wrong comments fixed:
   - `crates/core/src/lib.rs:7` "in progress" claim removed
   - `crates/traits/src/types.rs:264/273` (already truncated by Phase 40;
     verify no remaining corruption)
   - `crates/server/src/lib.rs:3-4` triple-header pattern collapsed
   - `crates/server/src/health.rs:3-4` triple-header pattern collapsed
6. CMT-06: `qwen3_config` deprecation shim updated — `since = "0.21.0"` →
   "0.22.0" (last release before v23.0; semver-compatible) OR shim deleted if
   no external consumers
7. `cargo doc --workspace --no-deps` produces 0 broken-link warnings
8. `cargo test --workspace --all-features` reports ≥ 1179 tests passing
9. `cargo clippy --workspace --all-targets --all-features -- -D warnings` clean
10. `cargo fmt --all --check` clean
</acceptance_criteria>
<action>
1. Generate a list of all files containing placeholder patterns:
   ```bash
   grep -rlE "^//!?\s+[a-z_]+:\s+[a-z_][a-z_ ]+\.?\s*$" crates/ | sort
   ```
2. For CMT-01 (module-level `//! X: X.`):
   - Read each file's first few lines
   - Delete lines matching the placeholder pattern
   - Preserve any substantive module-level docs (multi-line, with explanations)
3. For CMT-02 (function-level `/// X: X.`):
   - Delete lines matching the placeholder pattern
   - Preserve any docs with parameters, returns, errors, examples, or substantive
     behavior descriptions
4. For CMT-03: Confirm zero occurrences:
   ```bash
   grep -rn "builder: construct via builder for documented field ergonomics" crates/
   ```
   If zero, mark complete; otherwise replace with type-specific docs.
5. For CMT-04: Strip phase IDs from rustdoc-only lines (not Rust code/comments
   that reference phases as design context):
   ```bash
   grep -rEn "///.*v(1[5-9]|2[0-3])\.[0-9]+|///.*Plan [0-9]+\.[0-9]+|///.*SEC-[0-9]+|///.*PERF-[0-9]+|///.*ARF-[0-9]+|///.*OPS-[0-9]+|///.*DOC-[0-9]+|///.*CMT-[0-9]+|///.*ARCH-[0-9]+|///.*RFU-[0-9]+|///.*FINAL-[0-9]+|///.*CODE-[0-9]+" crates/ | head -50
   ```
   Review each match; strip the ID, keep any substantive text.
6. For CMT-05:
   - `crates/core/src/lib.rs:7`: change `//! - Speculative decoding (in progress)`
     to `//! - Speculative decoding (v18.0+; production-ready)`
   - `crates/traits/src/types.rs:264/273`: verify no remaining corruption
     (file is now ~100 lines after Phase 40; this finding may be obsolete)
   - `crates/server/src/lib.rs:3-4`: delete the redundant `// crates/server/src/lib.rs`
     line, keep `//! vLLM server crate - HTTP API server for LLM inference`
   - `crates/server/src/health.rs:3-4`: same pattern
7. For CMT-06:
   - Check if `qwen3_config` shim has external consumers:
     `grep -rn "vllm_model::qwen3_config" crates/`
   - If only the shim itself and internal references, update `since` to "0.22.0"
     (more accurate — this was the prior version when v21.0 closed, and v22.0
     shipped without changes to this module)
   - If zero consumers remain, delete the shim entirely
8. Run `cargo doc --workspace --no-deps` to verify no broken-link warnings
9. Run `cargo test --workspace --all-features` to verify 1179+ tests still pass
10. Run `cargo clippy --workspace --all-targets --all-features -- -D warnings`
11. Run `cargo fmt --all` to normalize formatting
</action>
<verify>
- `grep -rEn "^//!?\s+[a-z_]+:\s+[a-z_][a-z_ ]+\.?\s*$" crates/` returns 0 lines
- `grep -rn "in progress" crates/core/src/lib.rs` returns nothing
- `grep -rn "builder: construct via builder" crates/` returns nothing (CMT-03)
- `cargo doc --workspace --no-deps` exits 0
- `cargo test --workspace --all-features` reports ≥ 1179 pass
- `cargo clippy --workspace --all-targets --all-features -- -D warnings` exits 0
- `cargo fmt --all --check` exits 0
</verify>
</task>

</tasks>

<verification>
## must_haves

### truths
- [ ] Zero placeholder module docs
- [ ] Zero placeholder function docs
- [ ] 4 wrong comments fixed
- [ ] qwen3_config shim resolved

### artifacts
- [ ] 30+ files modified (placeholder removal)
- [ ] crates/core/src/lib.rs (in-progress fix)
- [ ] crates/server/src/{lib,health}.rs (triple-header fix)
- [ ] crates/model/src/lib.rs (qwen3_config shim)

### key_links
- [ ] `cargo doc --workspace --no-deps` clean
- [ ] `cargo test --workspace --all-features` ≥ 1179 pass
- [ ] `cargo clippy` clean
</verification>

<requirements>
- CMT-01
- CMT-02
- CMT-03
- CMT-04
- CMT-05
- CMT-06
</requirements>
