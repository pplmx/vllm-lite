# Archived Benchmark Artifacts

Criterion HTML/JSON reports were moved to:

```text
docs/archive/benchmark-results/
```

Re-run benchmarks locally with `cargo bench` (see `docs/perf/`). Generated output should not be committed — see root `.gitignore`.
