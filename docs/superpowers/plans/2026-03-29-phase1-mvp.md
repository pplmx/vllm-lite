# vLLM-lite Phase 1: Minimum Viable Version

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** End-to-end single-request inference with FakeModel, basic HTTP API, no batching.

**Architecture:** 3-crate Cargo workspace: `core` (types, scheduler, engine, sampling) → `model` (Model trait, FakeModel) → `server` (axum HTTP). Single-threaded engine worker with tokio channels.

**Tech Stack:** Rust, tokio, axum, thiserror, anyhow, rand

**Spec:** `docs/superpowers/specs/2026-03-29-vllm-lite-design.md`

---

## File Map

```text
vllm-lite/
├── Cargo.toml                          # workspace root
├── crates/
│   ├── core/
│   │   ├── Cargo.toml
│   │   └── src/
│   │       ├── lib.rs
│   │       ├── types.rs
│   │       ├── kv_cache.rs
│   │       ├── scheduler.rs
│   │       ├── engine.rs
│   │       ├── sampling.rs
│   │       └── error.rs
│   ├── model/
│   │   ├── Cargo.toml
│   │   └── src/
│   │       ├── lib.rs
│   │       └── fake.rs
│   └── server/
│       ├── Cargo.toml
│       └── src/
│           ├── main.rs
│           └── api.rs
```

---

### Task 1: Clean up + workspace scaffold

**Files:**

- Delete: `src/` (entire directory)
- Delete: `spec.md`, `layout.md`
- Create: `Cargo.toml` (workspace root)
- Create: `crates/core/Cargo.toml`
- Create: `crates/model/Cargo.toml`
- Create: `crates/server/Cargo.toml`
- Create: stub `lib.rs` / `main.rs` in each crate

- [ ] **Step 1: Delete old files**

```bash
rm -rf src/ spec.md layout.md
```

- [ ] **Step 2: Create workspace root Cargo.toml**

```toml
[workspace]
members = ["crates/core", "crates/model", "crates/server"]
resolver = "2"
```

- [ ] **Step 3: Create core crate Cargo.toml**

```toml
[package]
name = "vllm-core"
version = "0.1.0"
edition = "2021"

[dependencies]
thiserror = "2"
```

- [ ] **Step 4: Create core crate stubs**

`crates/core/src/lib.rs`:

```rust
pub mod error;
pub mod types;
```

`crates/core/src/error.rs`:

```rust
#[derive(Debug, thiserror::Error)]
pub enum EngineError {
    #[error("generic error: {0}")]
    Generic(String),
}

pub type Result<T> = std::result::Result<T, EngineError>;
```

`crates/core/src/types.rs`:

```rust
pub type TokenId = u32;
pub type SeqId = u64;
pub type BlockId = usize;
```

- [ ] **Step 5: Create model crate Cargo.toml and stub**

`crates/model/Cargo.toml`:

```toml
[package]
name = "vllm-model"
version = "0.1.0"
edition = "2021"

[dependencies]
vllm-core = { path = "../core" }
rand = "0.10"
```

`crates/model/src/lib.rs`:

```rust
pub mod fake;
```

- [ ] **Step 6: Create server crate Cargo.toml and stub**

`crates/server/Cargo.toml`:

```toml
[package]
name = "vllm-server"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "vllm-server"
path = "src/main.rs"

[dependencies]
vllm-core = { path = "../core" }
vllm-model = { path = "../model" }
tokio = { version = "1", features = ["full"] }
axum = "0.8"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
```

`crates/server/src/main.rs`:

```rust
fn main() {
    println!("vllm-server starting...");
}
```

- [ ] **Step 7: Verify workspace compiles**

```bash
cargo check --workspace
```

Expected: all 3 crates compile with warnings (unused imports), no errors.

- [ ] **Step 8: Commit**

```bash
git add -A
git commit -m "chore: scaffold workspace with 3 crates"
```

---

### Task 2: Core types

**Files:**

- Modify: `crates/core/src/types.rs`
- Create: `crates/core/src/error.rs` (update existing stub)

- [ ] **Step 1: Define all core types**

`crates/core/src/types.rs`:

```rust
pub type TokenId = u32;
pub type SeqId = u64;
pub type BlockId = usize;

pub struct Request {
    pub id: SeqId,
    pub prompt: Vec<TokenId>,
    pub max_tokens: usize,
    pub sampling_params: SamplingParams,
}

impl Request {
    pub fn new(id: SeqId, prompt: Vec<TokenId>, max_tokens: usize) -> Self {
        Self {
            id,
            prompt,
            max_tokens,
            sampling_params: SamplingParams::default(),
        }
    }
}

#[derive(Clone, Debug)]
pub struct SamplingParams {
    pub temperature: f32,
    pub top_k: usize,
    pub top_p: f32,
}

impl Default for SamplingParams {
    fn default() -> Self {
        Self {
            temperature: 0.0,
            top_k: 0,
            top_p: 1.0,
        }
    }
}

#[derive(Clone, Debug)]
pub struct Sequence {
    pub id: SeqId,
    pub tokens: Vec<TokenId>,
    pub num_computed_tokens: usize,
    pub status: Status,
    pub max_tokens: usize,
    pub sampling_params: SamplingParams,
}

#[derive(Clone, Copy, PartialEq, Debug)]
pub enum Status {
    Waiting,
    Prefilling,
    Decoding,
    Finished,
}

pub struct Batch {
    pub seq_ids: Vec<SeqId>,
    pub input_tokens: Vec<Vec<TokenId>>,
    pub positions: Vec<Vec<usize>>,
}

impl Batch {
    pub fn is_empty(&self) -> bool {
        self.seq_ids.is_empty()
    }
}

pub struct BatchOutput {
    pub seq_ids: Vec<SeqId>,
    pub next_tokens: Vec<TokenId>,
}
```

- [ ] **Step 2: Update error module**

`crates/core/src/error.rs`:

```rust
#[derive(Debug, thiserror::Error)]
pub enum EngineError {
    #[error("sequence {id} not found")]
    SeqNotFound { id: u64 },

    #[error("model forward failed: {0}")]
    ModelError(String),

    #[error("sampling failed: {0}")]
    SamplingError(String),
}

pub type Result<T> = std::result::Result<T, EngineError>;
```

- [ ] **Step 3: Update lib.rs**

`crates/core/src/lib.rs`:

```rust
pub mod error;
pub mod types;
```

- [ ] **Step 4: Verify compiles**

```bash
cargo check --workspace
```

Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add -A
git commit -m "feat(core): define Request, Sequence, Batch, SamplingParams types"
```

---

### Task 3: Scheduler (single sequence)

**Files:**

- Create: `crates/core/src/scheduler.rs`
- Modify: `crates/core/src/lib.rs`

- [ ] **Step 1: Write failing test for Scheduler**

`crates/core/src/scheduler.rs`:

```rust
use crate::types::{Request, Sequence, Status, Batch, SeqId, TokenId};
use std::collections::VecDeque;

pub struct Scheduler {
    waiting: VecDeque<Sequence>,
    running: Vec<Sequence>,
    finished: Vec<Sequence>,
    next_seq_id: SeqId,
}

impl Scheduler {
    pub fn new() -> Self {
        Self {
            waiting: VecDeque::new(),
            running: Vec::new(),
            finished: Vec::new(),
            next_seq_id: 1,
        }
    }

    pub fn add_request(&mut self, req: Request) -> SeqId {
        let id = if req.id == 0 {
            let id = self.next_seq_id;
            self.next_seq_id += 1;
            id
        } else {
            req.id
        };

        let seq = Sequence {
            id,
            tokens: req.prompt,
            num_computed_tokens: 0,
            status: Status::Waiting,
            max_tokens: req.max_tokens,
            sampling_params: req.sampling_params,
        };
        self.waiting.push_back(seq);
        id
    }

    pub fn build_batch(&mut self) -> Batch {
        // Move waiting → running
        while let Some(mut seq) = self.waiting.pop_front() {
            if seq.status == Status::Waiting {
                seq.status = Status::Prefilling;
            }
            self.running.push(seq);
        }

        let mut seq_ids = vec![];
        let mut input_tokens = vec![];
        let mut positions = vec![];

        for seq in &self.running {
            if seq.status == Status::Finished {
                continue;
            }

            let (tokens, pos) = if seq.status == Status::Prefilling {
                // Prefill: process all tokens not yet computed
                let start = seq.num_computed_tokens;
                let tokens = seq.tokens[start..].to_vec();
                let pos: Vec<usize> = (start..seq.tokens.len()).collect();
                (tokens, pos)
            } else {
                // Decode: only the last token
                let last = *seq.tokens.last().unwrap();
                let pos = seq.tokens.len() - 1;
                (vec![last], vec![pos])
            };

            seq_ids.push(seq.id);
            input_tokens.push(tokens);
            positions.push(pos);
        }

        Batch {
            seq_ids,
            input_tokens,
            positions,
        }
    }

    pub fn update(&mut self, seq_ids: &[SeqId], next_tokens: &[TokenId]) {
        for (seq_id, token) in seq_ids.iter().zip(next_tokens) {
            if let Some(seq) = self.running.iter_mut().find(|s| s.id == *seq_id) {
                if seq.status == Status::Prefilling {
                    seq.num_computed_tokens = seq.tokens.len();
                    seq.status = Status::Decoding;
                }

                seq.tokens.push(*token);

                if seq.tokens.len() >= seq.max_tokens {
                    seq.status = Status::Finished;
                }
            }
        }

        // Move finished → finished list
        let mut i = 0;
        while i < self.running.len() {
            if self.running[i].status == Status::Finished {
                let seq = self.running.remove(i);
                self.finished.push(seq);
            } else {
                i += 1;
            }
        }
    }

    pub fn has_pending(&self) -> bool {
        !self.waiting.is_empty() || !self.running.is_empty()
    }

    pub fn finished_sequences(&self) -> &[Sequence] {
        &self.finished
    }
}
```

- [ ] **Step 2: Write unit test**

Add at the bottom of `crates/core/src/scheduler.rs`:

```rust
#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn test_add_and_build_batch_prefill() {
        let mut sched = Scheduler::new();
        sched.add_request(Request::new(1, vec![10, 20, 30], 5));

        let batch = sched.build_batch();
        assert_eq!(batch.seq_ids.len(), 1);
        assert_eq!(batch.input_tokens[0], vec![10, 20, 30]);
        assert_eq!(batch.positions[0], vec![0, 1, 2]);
    }

    #[test]
    fn test_update_transitions_to_decode() {
        let mut sched = Scheduler::new();
        sched.add_request(Request::new(1, vec![10, 20, 30], 5));

        let batch = sched.build_batch();
        sched.update(&batch.seq_ids, &[99]);

        // After prefill update, next batch should be decode (last token only)
        let batch2 = sched.build_batch();
        assert_eq!(batch2.input_tokens[0], vec![99]);
        assert_eq!(batch2.positions[0], vec![3]); // tokens now [10,20,30,99], position 3
    }

    #[test]
    fn test_finished_after_max_tokens() {
        let mut sched = Scheduler::new();
        sched.add_request(Request::new(1, vec![10], 3)); // max_tokens=3, already 1 token

        // Step 1: prefill
        let batch = sched.build_batch();
        sched.update(&batch.seq_ids, &[20]); // tokens: [10, 20]

        // Step 2: decode
        let batch = sched.build_batch();
        sched.update(&batch.seq_ids, &[30]); // tokens: [10, 20, 30] → finished

        assert!(!sched.has_pending());
        assert_eq!(sched.finished_sequences().len(), 1);
    }
}
```

- [ ] **Step 3: Run tests to verify they pass**

```bash
cargo test -p vllm-core -- scheduler
```

Expected: 3 passed

- [ ] **Step 4: Update lib.rs**

`crates/core/src/lib.rs`:

```rust
pub mod error;
pub mod types;
pub mod scheduler;
```

- [ ] **Step 5: Commit**

```bash
git add -A
git commit -m "feat(core): implement Scheduler with prefill/decode lifecycle"
```

---

### Task 4: Sampling (greedy)

**Files:**

- Create: `crates/core/src/sampling.rs`
- Modify: `crates/core/src/lib.rs`

- [ ] **Step 1: Write greedy_sample with test**

`crates/core/src/sampling.rs`:

```rust
use crate::types::TokenId;

pub fn greedy_sample(logits: &[f32]) -> TokenId {
    logits
        .iter()
        .enumerate()
        .max_by(|(_, a), (_, b)| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal))
        .map(|(i, _)| i as TokenId)
        .unwrap_or(0)
}

pub fn sample_batch(logits_list: &[Vec<f32>], temperature: f32) -> Vec<TokenId> {
    logits_list
        .iter()
        .map(|logits| {
            if temperature == 0.0 {
                greedy_sample(logits)
            } else {
                greedy_sample(logits) // TODO: temperature sampling in Phase 2
            }
        })
        .collect()
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn test_greedy_selects_max() {
        assert_eq!(greedy_sample(&[0.1, 0.5, 0.3]), 1);
    }

    #[test]
    fn test_greedy_first_on_tie() {
        assert_eq!(greedy_sample(&[0.5, 0.5, 0.3]), 0);
    }

    #[test]
    fn test_greedy_empty() {
        assert_eq!(greedy_sample(&[]), 0);
    }

    #[test]
    fn test_sample_batch() {
        let logits = vec![vec![0.1, 0.9], vec![0.8, 0.2]];
        assert_eq!(sample_batch(&logits, 0.0), vec![1, 0]);
    }
}
```

- [ ] **Step 2: Run tests**

```bash
cargo test -p vllm-core -- sampling
```

Expected: 4 passed

- [ ] **Step 3: Update lib.rs**

`crates/core/src/lib.rs`:

```rust
pub mod error;
pub mod types;
pub mod scheduler;
pub mod sampling;
```

- [ ] **Step 4: Commit**

```bash
git add -A
git commit -m "feat(core): implement greedy sampling"
```

---

### Task 5: Model trait + FakeModel

**Files:**

- Modify: `crates/model/src/lib.rs`
- Modify: `crates/model/src/fake.rs`

- [ ] **Step 1: Define Model trait and FakeModel**

`crates/model/src/lib.rs`:

```rust
pub mod fake;

use vllm_core::types::{Batch, BatchOutput};
use vllm_core::error::Result;

pub trait Model: Send {
    fn forward(&self, batch: &Batch) -> Result<BatchOutput>;
}
```

`crates/model/src/fake.rs`:

```rust
use rand::Rng;
use crate::Model;
use vllm_core::types::{Batch, BatchOutput, TokenId};
use vllm_core::error::Result;

pub struct FakeModel {
    vocab_size: usize,
}

impl FakeModel {
    pub fn new(vocab_size: usize) -> Self {
        Self { vocab_size }
    }
}

impl Model for FakeModel {
    fn forward(&self, batch: &Batch) -> Result<BatchOutput> {
        let mut rng = rand::rng();
        let next_tokens: Vec<TokenId> = batch
            .seq_ids
            .iter()
            .map(|_| rng.random_range(0..self.vocab_size) as TokenId)
            .collect();

        Ok(BatchOutput {
            seq_ids: batch.seq_ids.clone(),
            next_tokens,
        })
    }
}
```

- [ ] **Step 2: Verify compiles**

```bash
cargo check --workspace
```

Expected: PASS

- [ ] **Step 3: Commit**

```bash
git add -A
git commit -m "feat(model): define Model trait and FakeModel"
```

---

### Task 6: Engine

**Files:**

- Create: `crates/core/src/engine.rs`
- Modify: `crates/core/src/lib.rs`

- [ ] **Step 1: Write Engine with test**

`crates/core/src/engine.rs`:

```rust
use crate::scheduler::Scheduler;
use crate::sampling::sample_batch;
use crate::types::{Request, BatchOutput, SeqId, TokenId};
use crate::error::Result;

pub trait ModelBackend: Send {
    fn forward(
        &self,
        seq_ids: &[SeqId],
        input_tokens: &[Vec<TokenId>],
        positions: &[Vec<usize>],
    ) -> Result<BatchOutput>;
}

pub struct Engine<M: ModelBackend> {
    pub scheduler: Scheduler,
    pub model: M,
}

impl<M: ModelBackend> Engine<M> {
    pub fn new(model: M) -> Self {
        Self {
            scheduler: Scheduler::new(),
            model,
        }
    }

    pub fn add_request(&mut self, req: Request) {
        self.scheduler.add_request(req);
    }

    pub fn step(&mut self) -> Result<Vec<(SeqId, TokenId)>> {
        let batch = self.scheduler.build_batch();
        if batch.is_empty() {
            return Ok(vec![]);
        }

        let output = self.model.forward(
            &batch.seq_ids,
            &batch.input_tokens,
            &batch.positions,
        )?;

        let tokens = sample_batch(
            &[output.next_tokens.iter().map(|&t| t as f32).collect()],
            0.0,
        );

        // For single-sequence: map back tokens
        let result: Vec<(SeqId, TokenId)> = batch
            .seq_ids
            .iter()
            .zip(output.next_tokens.iter())
            .map(|(&id, &token)| (id, token))
            .collect();

        self.scheduler.update(&batch.seq_ids, &output.next_tokens);
        Ok(result)
    }

    pub fn has_pending(&self) -> bool {
        self.scheduler.has_pending()
    }
}
```

- [ ] **Step 2: Update lib.rs**

`crates/core/src/lib.rs`:

```rust
pub mod error;
pub mod types;
pub mod scheduler;
pub mod sampling;
pub mod engine;
```

- [ ] **Step 3: Update model crate to implement ModelBackend**

`crates/model/src/lib.rs`:

```rust
pub mod fake;

use vllm_core::types::{Batch, BatchOutput, SeqId, TokenId};
use vllm_core::error::Result;
use vllm_core::engine::ModelBackend;

pub trait Model: Send {
    fn forward(&self, batch: &Batch) -> Result<BatchOutput>;
}
```

`crates/model/src/fake.rs`:

```rust
use rand::Rng;
use vllm_core::types::{SeqId, TokenId, BatchOutput};
use vllm_core::engine::ModelBackend;
use vllm_core::error::Result;

pub struct FakeModel {
    vocab_size: usize,
}

impl FakeModel {
    pub fn new(vocab_size: usize) -> Self {
        Self { vocab_size }
    }
}

impl ModelBackend for FakeModel {
    fn forward(
        &self,
        seq_ids: &[SeqId],
        _input_tokens: &[Vec<TokenId>],
        _positions: &[Vec<usize>],
    ) -> Result<BatchOutput> {
        let mut rng = rand::rng();
        let next_tokens: Vec<TokenId> = seq_ids
            .iter()
            .map(|_| rng.random_range(0..self.vocab_size) as TokenId)
            .collect();

        Ok(BatchOutput {
            seq_ids: seq_ids.to_vec(),
            next_tokens,
        })
    }
}
```

- [ ] **Step 4: Write engine integration test**

Add to `crates/core/src/engine.rs`:

```rust
#[cfg(test)]
mod tests {
    use super::*;
    use crate::types::{Request, SamplingParams};

    struct StubModel {
        token_to_return: TokenId,
    }

    impl ModelBackend for StubModel {
        fn forward(
            &self,
            seq_ids: &[SeqId],
            _input_tokens: &[Vec<TokenId>],
            _positions: &[Vec<usize>],
        ) -> Result<BatchOutput> {
            Ok(BatchOutput {
                seq_ids: seq_ids.to_vec(),
                next_tokens: seq_ids.iter().map(|_| self.token_to_return).collect(),
            })
        }
    }

    #[test]
    fn test_engine_single_request() {
        let mut engine = Engine::new(StubModel { token_to_return: 42 });
        engine.add_request(Request::new(1, vec![10, 20], 4));

        // Step 1: prefill
        let out = engine.step().unwrap();
        assert_eq!(out.len(), 1);
        assert_eq!(out[0], (1, 42));

        // Step 2: decode
        let out = engine.step().unwrap();
        assert_eq!(out[0], (1, 42));

        // Step 3: decode → reaches max_tokens=4
        let out = engine.step().unwrap();
        assert_eq!(out[0], (1, 42));

        // Step 4: no more pending
        assert!(!engine.has_pending());
    }
}
```

- [ ] **Step 5: Run tests**

```bash
cargo test -p vllm-core
```

Expected: all tests pass

- [ ] **Step 6: Commit**

```bash
git add -A
git commit -m "feat(core): implement Engine with step loop"
```

---

### Task 7: HTTP Server (basic, non-streaming)

**Files:**

- Modify: `crates/server/src/main.rs`
- Create: `crates/server/src/api.rs`

- [ ] **Step 1: Write API request/response types and handler**

`crates/server/src/api.rs`:

```rust
use axum::{extract::State, Json};
use serde::{Deserialize, Serialize};
use std::sync::{Arc, Mutex};
use tokio::sync::oneshot;
use vllm_core::types::{Request, SamplingParams, TokenId};
use vllm_model::fake::FakeModel;
use vllm_core::engine::{Engine, ModelBackend};

pub type EngineHandle = Arc<Mutex<Engine<FakeModel>>>;

#[derive(Deserialize)]
pub struct CompletionRequest {
    pub prompt: String,
    #[serde(default = "default_max_tokens")]
    pub max_tokens: usize,
}

fn default_max_tokens() -> usize {
    100
}

#[derive(Serialize)]
pub struct CompletionResponse {
    pub text: String,
    pub tokens: Vec<u32>,
    pub num_tokens: usize,
}

pub async fn completions(
    State(engine): State<EngineHandle>,
    Json(req): Json<CompletionRequest>,
) -> Json<CompletionResponse> {
    // Simple tokenization: split by space, hash each word to u32
    let tokens: Vec<TokenId> = req
        .prompt
        .split_whitespace()
        .enumerate()
        .map(|(i, _)| (i + 1) as TokenId)
        .collect();

    let request = Request::new(1, tokens.clone(), tokens.len() + req.max_tokens);

    // Run engine synchronously in blocking task
    let engine_clone = engine.clone();
    let result = tokio::task::spawn_blocking(move || {
        let mut eng = engine_clone.lock().unwrap();
        eng.add_request(request);

        let mut generated: Vec<TokenId> = Vec::new();
        while eng.has_pending() {
            if let Ok(outputs) = eng.step() {
                for (_, token) in outputs {
                    generated.push(token);
                }
            }
        }
        generated
    })
    .await
    .unwrap();

    let all_tokens: Vec<TokenId> = tokens.iter().chain(result.iter()).copied().collect();

    Json(CompletionResponse {
        text: format!("Generated {} tokens", result.len()),
        tokens: all_tokens,
        num_tokens: result.len(),
    })
}
```

- [ ] **Step 2: Write server main**

`crates/server/src/main.rs`:

```rust
mod api;

use axum::{routing::post, Router};
use std::sync::{Arc, Mutex};
use vllm_core::engine::Engine;
use vllm_model::fake::FakeModel;

#[tokio::main]
async fn main() {
    let model = FakeModel::new(32000);
    let engine = Engine::new(model);
    let state = Arc::new(Mutex::new(engine));

    let app = Router::new()
        .route("/v1/completions", post(api::completions))
        .with_state(state);

    let listener = tokio::net::TcpListener::bind("0.0.0.0:8000").await.unwrap();
    println!("vllm-lite listening on http://0.0.0.0:8000");
    axum::serve(listener, app).await.unwrap();
}
```

- [ ] **Step 3: Verify compiles**

```bash
cargo check --workspace
```

Expected: PASS

- [ ] **Step 4: Manual test**

```bash
# Terminal 1
cargo run -p vllm-server

# Terminal 2
curl -X POST http://localhost:8000/v1/completions \
  -H "Content-Type: application/json" \
  -d '{"prompt": "hello world", "max_tokens": 5}'
```

Expected: JSON response with generated tokens.

- [ ] **Step 5: Commit**

```bash
git add -A
git commit -m "feat(server): basic OpenAI-compatible /v1/completions endpoint"
```

---

### Task 8: Workspace cleanup + CI

**Files:**

- Modify: `crates/core/src/lib.rs`
- Modify: `crates/model/src/lib.rs`

- [ ] **Step 1: Remove unused Model trait from model crate**

`crates/model/src/lib.rs`:

```rust
pub mod fake;
```

Remove the `pub trait Model` since we're using `ModelBackend` from core instead.

- [ ] **Step 2: Full test suite**

```bash
cargo test --workspace
```

Expected: all tests pass

- [ ] **Step 3: Clippy check**

```bash
cargo clippy --workspace -- -D warnings
```

Fix any warnings.

- [ ] **Step 4: Final commit**

```bash
git add -A
git commit -m "chore: cleanup, pass clippy"
```

---

## Verification

After all tasks:

```bash
# Build
cargo build --workspace

# Test
cargo test --workspace

# Lint
cargo clippy --workspace -- -D warnings

# Run
cargo run -p vllm-server
# In another terminal:
curl -X POST http://localhost:8000/v1/completions \
  -H "Content-Type: application/json" \
  -d '{"prompt": "hello world", "max_tokens": 3}'
```

Expected output:

```json
{"text":"Generated 3 tokens","tokens":[1,2,3,4,5],"num_tokens":3}
```

## Spec Coverage

| Spec Section                  | Covered By  |
| ----------------------------- | ----------- |
| Core Types (Sec 4)            | Task 2      |
| Scheduler (Sec 5)             | Task 3      |
| Sampling (Sec 13)             | Task 4      |
| Model trait (Sec 7)           | Task 5      |
| Engine (Sec 4.1)              | Task 6      |
| HTTP API (Sec overview)       | Task 7      |
| KV Cache (Sec 6)              | **Phase 2** |
| Chunked Prefill (Sec 12.1)    | **Phase 2** |
| Continuous Batching (Sec 5.2) | **Phase 2** |
| Paged Attention (Sec 8)       | **Phase 4** |
| Real Model (Sec 7)            | **Phase 5** |

Phase 1 intentionally skips KV cache (uses contiguous tokens), real batching (single sequence), and real model. These come in subsequent phases.
